# Eden 漫画风个人作品集 · 交付说明（一页纸）

## 一句话

为 Eden 搭建的**漫画 / 美漫风格个人作品集单页网站**，纯静态、零依赖，双击 `index.html` 即可打开，改文字即可上线。

---

## 交付物

```
E:\工作台\个人网站\
├── index.html          # 单页 7 板块（首页 / 关于我 / 项目 / 博客 / 健身 / 故事 / 联系）
├── css\style.css       # 全部样式，颜色/间距/圆角/阴影全部 CSS 变量化
├── js\main.js          # 全部交互，数据数组集中在文件顶部
├── images\README.md    # 换照片的分步指南 + 文件名对照表
├── 使用说明.md          # 面向零基础用户的内容替换 + 部署指南
└── overview.md         # 本文件
```

**技术栈**：HTML5 + CSS3 + 原生 JavaScript（ES5 语法，无框架、无构建、无 npm 包）
**字体**：Google Fonts CDN（Bangers / Noto Sans SC / JetBrains Mono），带系统字体兜底，加载失败不影响排版
**图标**：38 个内联 SVG `<symbol>`，无图标字体、无 CDN 依赖，断网可用

---

## 7 个板块实现要点

| # | 板块 | 实现要点 |
|---|---|---|
| 1 | **首页 `#home`** | 半调网点 + 角落速度线背景；左侧大标题 + `HEY!` 爆炸拟声词；右侧拍立得头像 + 对话气泡；2 个 CTA；6 格漫画快速导航（内联 SVG 图标）；3 张精选作品卡（hover 浮层 + 对话气泡）；4 类技能树带熟练度进度条 |
| 2 | **关于我 `#about`** | 真实故事三段自述（我是谁 / 我做什么 / 我相信什么）；照片 + 基本信息卡；4 类技能贴纸；6 节点垂直成长轨迹时间线（带状态药丸：摆烂期→谷底→转折点→上升期→成就达成→进行中）；八小时之外 4 格；5 个「隐藏成就」彩蛋气泡 |
| 3 | **项目作品 `#projects`** | 5 类筛选（全部/前端/后端/全栈/工具其他）实时过滤；3→2→1 列响应式网格；4 个 `{{}}` 占位骨架；点击卡片弹出漫画风详情模态框（背景/架构/职责/成果/链接） |
| 4 | **技术博客 `#blog`** | 实时标题搜索（防抖 160ms）+ 4 类筛选 + 分页（每页 6 篇，页码自动生成）；5 篇文章卡（左封面右内容）；「阅读全文」弹出正文模态框 |
| 5 | **健身记录 `#fitness`** | 4 个数据卡；**纯 SVG 手绘双 Y 轴折线图**（虚线网格 + 星星数据点，数据改 `js/main.js` 顶部数组）；周一到周日训练周历；5 个 PR 奖牌；笔记本横格心得卡；碳循环饮食说明 + 饮食模板表 |
| 6 | **我们的故事 `#story`** | 心形（CSS mask）大合照框；6 节点恋爱时间线（爱心连接）；15 格错落照片墙（hover 弹出说明气泡 + 爱心/星星贴纸）；6 格共同爱好；4 个 WISHLIST 目的地（已打卡印章）；「故事还在继续」结尾 |
| 7 | **联系我 `#contact`** | 4 张联系卡（邮箱/GitHub/微信二维码/所在地），点击即复制（`navigator.clipboard` + `execCommand` 兜底 + `prompt` 再兜底）；漫画风表单（必填 + 邮箱格式校验，mailto 提交，含 Formspree 替换注释）；6 个内联 SVG 社交贴纸；简历下载大卡；4 条 FAQ 手风琴（原生 `<details>`） |

**页脚**：品牌区 + 社交图标 + 签名档「摆烂了四年，然后用 AI 重新开局 —— 种一棵树最好的时间是现在」。

---

## 交互与动效（全部原生，零库）

- 滚动渐入：`IntersectionObserver` + `translateY/opacity`
- 首屏依次弹入：CSS `@keyframes popIn` + 递增 `animation-delay`
- 导航栏：滚动 >20px 变「漫画贴纸」（加粗描边 + 硬阴影），当前板块自动高亮
- 汉堡菜单：手机端展开/收起，点击链接自动关闭，Esc / 点遮罩关闭
- 回到顶部：滚过 0.8 屏出现，漫画「起飞」按钮
- 卡片 hover：上浮 6px + 阴影加深（已用更高优先级规则覆盖渐入动画的 transform）
- **深色模式**：`data-theme` 切换 + CSS 变量，`localStorage` 记忆，跟随系统偏好；太阳/月亮图标
- Toast 轻提示：复制成功、校验失败、主题切换反馈

---

## 无障碍

- 语义化标签（`header/nav/main/section/footer`）+ `aria-label` / `aria-expanded` / `aria-modal`
- 跳转到主内容的 skip-link
- 项目卡可 Tab 聚焦，回车/空格打开详情
- 模态框支持 Esc 关闭、关闭后焦点归位
- **`@media (prefers-reduced-motion: reduce)` 关闭全部动画**
- 键盘可见焦点框 `:focus-visible`

---

## 响应式断点

- **> 1024px**：桌面，网格 3~5 列，完整导航
- **≤ 1024px**：平板，大部分网格降为 2 列，首屏改上下布局
- **≤ 720px**：手机，网格降为 1 列，导航收成汉堡菜单
- **≤ 400px**：超小屏，PR / 周历降为 1 列

---

## 需要注意的地方

1. **占位符体系**：所有待填内容统一用 `{{...}}` 包裹，所有需要改的位置统一标 `✏️` 注释 —— 全局搜索 `✏️` 或 `{{` 即可找全。
2. **图片全部是 CSS 占位块**，不使用 picsum 等外链（避免加载失败被误认为网站坏了）。换成真实照片的方法写在 `images/README.md`。
3. **`file://` 直接双击可用**：没有用任何需要 HTTP 协议的 API（无 fetch、无 ES module）。`localStorage` 与 `navigator.clipboard` 都做了 try/catch 或降级兜底。
4. **简历**：需用户自行把 PDF 命名为 `resume.pdf` 放在根目录（首页 CTA 和联系页下载按钮都指向它）。
5. **联系表单走 mailto 兜底**（纯静态站无后端），`js/main.js` 里已写明如何替换为 Formspree。
6. **健身图表**：数据改 `js/main.js` 顶部的 `bodyData` 数组，折线图自动重绘；切换主题后图表颜色也会跟着重绘。
7. **博客分页**：默认每页 6 篇；现有 5 篇占位文章时只显示 1 页，加到 7 篇以上会自动出现第 2 页。

---

## 部署

纯静态文件，无需构建。`npm run build` 不存在也不需要有。

- **推荐新手**：Vercel / Netlify 拖拽文件夹 → 30 秒上线
- **经典方案**：GitHub Pages（仓库名 `用户名.github.io`）

三种方案的分步骤图解写在 `使用说明.md` 第六章。
