# 📖 使用说明 · Eden 个人作品集

> 这份文档是写给**完全不懂技术**的人看的。
> 你不需要安装任何软件，也不需要懂代码 —— 跟着做就行。
> 遇到看不懂的名词，直接跳过，照着点鼠标即可。

---

## 目录

1. [怎么预览网站](#一怎么预览网站)
2. [内容替换清单（最重要）](#二内容替换清单最重要)
3. [怎么换照片](#三怎么换照片)
4. [怎么改颜色和字体](#四怎么改颜色和字体)
5. [怎么加项目 / 加文章](#五怎么加项目--加文章)
6. [免费部署上线（3 种方案）](#六免费部署上线3-种方案)
7. [常见问题排查](#七常见问题排查)

---

## 一、怎么预览网站

### 最简单的方法：双击

打开你存放网站的文件夹，找到 **`index.html`**，**双击它**。

默认浏览器会自动打开，网站就显示在眼前了 ✅

> 就这么简单。这个文件不需要联网、不需要服务器、不需要安装任何东西。

### 修改后怎么看效果？

每次改完代码，**保存文件**（`Ctrl + S`），然后回到浏览器按 **`F5`** 刷新。

如果刷新后没变化，试试 **`Ctrl + F5`**（强制刷新，忽略缓存）。

### 推荐用什么工具改代码？

- **零基础推荐**：Windows 自带的「记事本」就能改，但不方便看行号
- **更好用的免费工具**：搜索下载 **VS Code**（微软出的，免费），
  打开后代码有颜色、有行号，搜索也方便

---

## 二、内容替换清单（最重要）

### 🔑 万能搜索技巧

用 VS Code 或记事本打开 `index.html`，按 **`Ctrl + F`**，搜索这个符号：

```
✏️
```

**所有需要你修改的地方，都标了这个铅笔符号。**
点「查找下一个」一个个改就行，不会漏。

---

### 2.1 全站通用信息

| 要改什么 | 在哪个文件 | 搜索什么关键词 |
|---|---|---|
| 浏览器标签页的标题 | `index.html` | `<title>` |
| 网站简介（分享给别人时显示的文字） | `index.html` | `name="description"` |
| 你的名字（首页大标题） | `index.html` | `hero__name` |
| 首页那一句自我介绍 | `index.html` | `hero__subtitle` |
| 首页三个小数据（届别 / 身份 / 状态） | `index.html` | `hero__facts` |
| 联系表单的**收件邮箱**（重要！） | `js/main.js` | `MY_EMAIL` |
| 页脚的年份（一般会自动更新，不用管） | `index.html` | `id="year"` |

---

### 2.2 七个板块分别改哪里

| 板块 | 在 index.html 里搜索 | 主要要改的内容 |
|---|---|---|
| **1. 首页** | `id="home"` | 名字、自我介绍、6 个快速导航卡片的描述、3 个精选项目、技能进度条 |
| **2. 关于我** | `id="about"` | 基本信息卡（姓名/届别/专业/身份/城市）、三段自述、技能标签、6 个时间线节点、八小时之外、隐藏成就 |
| **3. 项目作品** | `id="projects"` | 4 个项目的名称 / 技术栈 / 简介 / 详情内容 |
| **4. 技术博客** | `id="blog"` | 5 篇文章的标题 / 日期 / 分类 / 摘要 / 正文 |
| **5. 健身记录** | `id="fitness"` | 4 个数据卡、折线图数据、一周训练计划、5 个 PR 成绩、心得、饮食表 |
| **6. 我们的故事** | `id="story"` | 两个人的名字、6 个时间线节点、照片墙说明、共同爱好、想去的地方 |
| **7. 联系我** | `id="contact"` | 邮箱、GitHub、微信号、城市、社交链接、FAQ 答案 |

---

### 2.3 所有 `{{}}` 占位符都要替换

在 `index.html` 里搜索 **一个左花括号加一个右花括号**：

```
{{
```

你会看到一堆像 `{{项目名称}}`、`{{your-email@example.com}}`、`{{XX}}` 这样的文字。
这些都是**等你填的空格**，全部替换成真实内容即可。

**特别要注意这几个：**

| 占位符 | 在哪儿 | 换成什么 |
|---|---|---|
| `{{your-email@example.com}}` | 联系卡片 + 页脚 + 表单 | 你的真实邮箱（搜 `your-email` 能一次找全） |
| `{{github.com/yourname}}` | 联系卡片 | 你的 GitHub 用户名 |
| `{{微信号}}` | 联系卡片 | 你的微信号 |
| `{{城市}}` | 联系卡片 + 关于我 | 你所在的城市 |
| `{{你的名字}} ♡ {{TA 的名字}}` | 我们的故事 | 你们俩的名字 |

---

### 2.4 改简历下载

1. 把你的简历 PDF 改名为 **`resume.pdf`**
2. 把它放到**网站根目录**（就是 `index.html` 所在的那一层，不是 `images` 里）
3. 搜索 `{{X}} 页`，把 `{{X}}` 改成你的简历页数

---

## 三、怎么换照片

**完整说明请看 `images/README.md`**，这里只说最简步骤：

1. 把照片放进 **`images`** 文件夹
2. 打开 `index.html`，搜索图片文件名（比如 `images/avatar.jpg`）
3. 找到这样的占位块，**整段删掉**：

```html
<div class="ph ph--avatar">
  <svg class="ph__icon" aria-hidden="true"><use href="#i-camera"/></svg>
  <span class="ph__label">你的照片</span>
  <span class="ph__hint">images/avatar.jpg</span>
</div>
```

4. 换成这一行：

```html
<img src="images/avatar.jpg" alt="我的照片">
```

5. 保存 + 刷新浏览器

> ⚠️ 文件名必须和 `images` 文件夹里的一模一样（包括大小写）。

---

## 四、怎么改颜色和字体

### 4.1 改颜色

打开 **`css/style.css`**，**最开头**有一段这样的代码：

```css
:root {
  --c-primary:   #FF6B35;   /* 主色 · 明亮橙 */
  --c-secondary: #4ECDC4;   /* 辅助 · 电光蓝 */
  --c-accent:    #FFE66D;   /* 点缀 · 柠檬黄 */
  --c-bg:        #FFF8F0;   /* 背景 · 米白 */
  --c-text:      #2D3436;   /* 文字 · 深灰 */
  --c-hot:       #FF6B9D;   /* 强调 · 玫红 */
  ...
}
```

**把 `#` 后面的 6 位数字换成你想要的颜色代码就行**，整站颜色会一起变。

去哪里找颜色代码？搜索「**颜色代码取色器**」，或者用这个现成的方案：

| 想换成 | 把 `--c-primary` 改成 |
|---|---|
| 番茄红 | `#E63946` |
| 天空蓝 | `#3A86FF` |
| 薄荷绿 | `#06D6A0` |
| 葡萄紫 | `#8338EC` |
| 阳光黄 | `#FFBE0B` |
| 深墨蓝 | `#1D3557` |

> 💡 只想改背景色？改 `--c-bg` 那一行的值就好。
> 改完记得保存 + `Ctrl + F5` 刷新。

**深色模式的颜色**在同文件靠后的 `[data-theme="dark"]` 里，改法一样。

### 4.2 改字体

同样在 `css/style.css` 顶部，找到这三行：

```css
--font-display: 'Bangers', 'Arial Black', ...;   /* 标题字体（漫画感） */
--font-body: 'Noto Sans SC', system-ui, ...;     /* 正文字体 */
--font-mono: 'JetBrains Mono', Consolas, ...;    /* 技术标签字体 */
```

- 想换**标题字体**：改 `--font-display` 第一个引号里的名字
- 想加**中文标题字体**：在 `--font-display` 里加上 `'Microsoft YaHei'`（已经加了）

改字体的完整步骤：

1. 打开 [fonts.google.com](https://fonts.google.com)，挑一个字体
2. 点右上角「Get font」→「Get embed code」
3. 复制 `<link>` 那一行
4. 打开 `index.html`，搜索 `fonts.googleapis.com`，把原有的那行 `<link>` 替换掉
5. 再回到 `css/style.css`，把 `--font-display` 里第一个名字改成字体的名字

> ⚠️ 标题字体建议保留 **Bangers**，它就是那种美漫炸裂感的关键。
> 如果你不喜欢，换成 **Luckiest Guy**、**Fredoka One**、**Permanent Marker** 也不错。

---

## 五、怎么加项目 / 加文章

### 5.1 加一个项目

打开 `index.html`，搜索：

```
data-category="全栈"
```

你会看到一整段代码，从 `<article class="project-card reveal"` 开始，
到 `</article>` 结束。**把这一整段复制一份，粘贴在它下面**，然后改这些地方：

```html
<article class="project-card reveal" data-tilt data-category="全栈"
  data-detail-bg="这里写项目背景"
  data-detail-tech="这里写技术架构"
  data-detail-role="这里写我的职责"
  data-detail-result="这里写项目成果"
  data-link-demo="https://你的在线预览地址"
  data-link-code="https://你的源码地址">

  <div class="project-card__media">
    <div class="ph ph--16x10">...</div>          <!-- 换成 <img src="images/xxx.jpg"> -->
    <span class="badge badge--floating">全栈</span>   <!-- 分类：前端 / 后端 / 全栈 / 工具其他 -->
    ...
  </div>

  <div class="project-card__body">
    <h3 class="project-card__title">项目名字</h3>
    <p class="project-card__desc">一句话简介</p>
    <div class="tag-row">
      <span class="tag">技术1</span><span class="tag">技术2</span>
    </div>
  </div>
</article>
```

**必改的 5 个地方：**

1. `data-category="全栈"` → 换成 `前端` / `后端` / `全栈` / `工具其他`（决定筛选栏里归到哪一类）
2. `<span class="badge badge--floating">全栈</span>` → 和上面保持一致
3. `data-detail-*` 四个属性 → 点开弹窗后显示的详细内容
4. `data-link-demo` / `data-link-code` → 没有就填 `#`，按钮会变成灰色的"未填写"状态
5. 标题、简介、技术标签

### 5.2 加一篇文章

搜索：

```
data-category="AI"
```

（注意：这个关键词在项目和博客里都有，**往下翻，找在 `id="blog"` 后面的那个**）

同样复制整段 `<article class="post-card reveal" ...> ... </article>`，改这些地方：

```html
<article class="post-card reveal" data-category="AI"
  data-content="这里写正文第一段。

这里写第二段（中间空一行就是分段）。">

  <div class="post-card__cover">
    <div class="ph ph--4x3">...</div>        <!-- 换成 <img src="images/blog-x.jpg"> -->
  </div>

  <div class="post-card__body">
    <div class="post-card__meta">
      <span class="badge">AI</span>          <!-- 分类：前端 / 后端 / AI / 生活随笔 -->
      <span class="meta-text">{{日期}}</span>
      <span class="meta-text">{{8}} 分钟阅读</span>
    </div>
    <h3 class="post-card__title">文章标题</h3>
    <p class="post-card__excerpt">文章摘要</p>
  </div>
</article>
```

**两个分类要一致**：`data-category="AI"` 和 `<span class="badge">AI</span>` 要写成一样的，
不然筛选会不生效。

**正文怎么换行**：在 `data-content="..."` 里面，想分段就**空一行**（敲两次回车）。

> 💡 文章超过 6 篇后，底部的分页按钮会自动出现。

### 5.3 加一条健身数据

打开 **`js/main.js`**，最开头就是数据区：

```js
var bodyData = [
  { month: '10月初', weight: 74.5, fat: 23.5 },
  { month: '10月中', weight: 73.8, fat: 23.0 },
  ...
];
```

照着格式加一行就行，比如 `{ month: '2月初', weight: 68.2, fat: 18.5 },`。
**折线图会自动重画**，你什么都不用改。

---

## 六、免费部署上线（3 种方案）

部署 = 把网站放到网上，让别人能通过网址访问。**全程免费**。

> 🌟 **新手推荐：方案二（Vercel）**，最简单，不用懂 Git，拖一下鼠标就上线。

---

### 方案一：GitHub Pages（最经典，完全免费）

**优点**：永久免费，和 GitHub 绑定，程序员圈子最常用
**缺点**：步骤稍多，需要注册 GitHub

#### 第 1 步：注册 GitHub

1. 打开 [github.com](https://github.com)
2. 点右上角 **Sign up**，用邮箱注册一个账号

#### 第 2 步：创建新仓库

1. 登录后，点右上角头像左边的 **「+」** → **New repository**
2. **Repository name** 填：`你的用户名.github.io`
   （比如你用户名叫 `eden-ai`，就填 `eden-ai.github.io`）
3. 选 **Public**（公开）
4. 勾上 **Add a README file**
5. 点绿色按钮 **Create repository**

#### 第 3 步：上传网站文件

1. 在仓库页面，点 **Add file** → **Upload files**
2. 把 `index.html`、`css` 文件夹、`js` 文件夹、`images` 文件夹**全部拖进去**
   > ⚠️ 注意保持文件夹结构：不要把 `css` 里的内容散着拖出来，要整个文件夹拖
3. 拉到页面最下方，点绿色按钮 **Commit changes**

#### 第 4 步：开启 Pages

1. 点仓库顶部的 **Settings**（设置）
2. 左侧菜单点 **Pages**
3. **Source** 选 **Deploy from a branch**
4. **Branch** 选 **main**，右边文件夹选 **/(root)**
5. 点 **Save**

#### 第 5 步：等 1~2 分钟，访问网址

打开 `https://你的用户名.github.io` —— 网站上线了 🎉

> 以后改了网站：回到仓库，**Add file → Upload files** 重新上传改过的文件即可（同名文件会覆盖）。

---

### 方案二：Vercel（最省事，推荐新手）

**优点**：不用懂 Git，可以直接拖文件夹，部署只要 1 分钟
**缺点**：需要注册（可用 GitHub 账号直接登录）

#### 详细步骤

1. 打开 [vercel.com](https://vercel.com)
2. 点右上角 **Sign Up**，选 **Continue with GitHub**（用 GitHub 账号登录最省事）
3. 登录后，点右上角 **Add New** → **Project**
4. 会让你导入 Git 仓库 —— **新手可以跳过**，看下面👇

#### 不用 Git 的方法（拖拽部署）

1. 先打开 [vercel.com](https://vercel.com) 并登录
2. 在打开的首页，直接把**整个网站文件夹**拖到浏览器中间的虚线框里
   （如果没有虚线框，就访问 [vercel.com/new](https://vercel.com/new)）
3. Vercel 会自动识别，点 **Deploy**
4. 等 30 秒左右，会给你一个网址，比如 `eden-portfolio.vercel.app`
5. 点网址就能看到你的网站了 🎉

> 以后改了网站：在 Vercel 项目页面点 **Deployments** → 把新文件夹拖进去重新部署。
> 或者更省事的做法：关联 GitHub 后，**改完代码推送到 GitHub，Vercel 会自动重新部署**。

---

### 方案三：Netlify（也很好用）

**优点**：拖拽部署，界面友好，免费额度充足
**缺点**：国内访问速度偶尔会慢一点

#### 详细步骤

1. 打开 [netlify.com](https://netlify.com)
2. 点右上角 **Sign up**，用 GitHub / 邮箱注册都行
3. 登录后，进入 **Sites** 页面
4. **把整个网站文件夹直接拖到页面中间的虚线框里**
   （页面上写着 "Drag and drop your site folder here"）
5. 等几秒钟，Netlify 会自动部署，并给你一个随机网址，
   比如 `sparkling-cupcake-123456.netlify.app`
6. 点 **Site settings** 可以改成你喜欢的名字，比如 `eden-portfolio.netlify.app` 🎉

> 以后改了网站：在 **Deploys** 页面，把新文件夹再拖进去一次即可。

---

### 三种方案怎么选？

| | GitHub Pages | **Vercel** | Netlify |
|---|---|---|---|
| 难度 | ⭐⭐⭐ | ⭐（最简单） | ⭐（最简单） |
| 需要 GitHub | 是 | 推荐有 | 不需要（邮箱也行） |
| 国内访问速度 | 中等 | 较快 | 中等 |
| 改完自动更新 | 需要重新上传 | ✅ 关联后自动 | 需要重新拖 |
| 适合谁 | 想学 Git 的人 | **新手首选** | 想最省事的人 |

---

## 七、常见问题排查

### ❓ 双击 index.html 打不开 / 显示空白

**可能原因 1：文件路径不对**
检查 `index.html`、`css`、`js`、`images` 是不是在**同一个文件夹**里。
正确结构应该是：

```
个人网站/
├── index.html
├── css/style.css
├── js/main.js
└── images/
```

如果多套了一层文件夹（比如 `个人网站/我的网站/index.html`），
浏览器就会找不到 `css/style.css`，页面会变成没有样式的纯文字。

**可能原因 2：用错了浏览器**
换 Chrome 或 Edge 打开试试（右键 `index.html` → 打开方式 → 选择 Chrome）。

---

### ❓ 页面是纯文字，没有颜色、没有排版

说明 **CSS 没加载**。

1. 确认 `css` 文件夹和 `index.html` **在同一层**
2. 确认 `css` 文件夹里的文件叫 **`style.css`**（不是 `style.css.txt`）
   > Windows 默认会隐藏文件后缀，看起来叫 `style.css`，实际可能是 `style.css.txt`。
   > 在文件夹顶部点「查看」→ 勾上「文件扩展名」就能看到真实名字。

---

### ❓ 点击导航、按钮没反应

说明 **JS 没加载或报错**。

1. 确认 `js` 文件夹和 `index.html` **在同一层**，文件名是 **`main.js`**
2. 按 `F12` 打开开发者工具 → 点 **Console**（控制台），
   如果看到**红色**的错误信息，把内容复制出来搜索一下

---

### ❓ 图片不显示

1. 确认照片真的放在了 `images` 文件夹里（不是它的子文件夹）
2. 确认代码里的文件名和实际文件名**完全一致**（包括大小写、`.jpg` / `.png`）
3. 打开 `images/README.md`，里面有一份完整的排查清单

---

### ❓ 手机上显示不正常 / 排版乱了

1. 先用 `Ctrl + F5` 强制刷新（手机浏览器有缓存）
2. 如果是在手机上打开本地文件 —— 手机一般打不开本地 HTML，
   需要**先部署上线**（看第六章），再用手机访问网址
3. 如果是长单词撑破了屏幕 —— 检查是不是有超长的英文没断开

---

### ❓ 我改了代码，刷新后没变化

浏览器缓存了旧版本。按 **`Ctrl + F5`**（Mac 是 `Cmd + Shift + R`）强制刷新。

---

### ❓ 深色模式下看不清文字

这是主题配色的问题。打开 `css/style.css`，搜索：

```
[data-theme="dark"]
```

里面的 `--c-text` 控制深色模式的文字颜色，调浅一点（比如 `#FFFFFF`）就好。

---

### ❓ 提交留言后没反应 / 打开了邮件客户端

**这是正常的。**

纯静态网站没有服务器，所以留言功能是用「打开你的邮件客户端」来实现的。
访问者点「发送消息」后，会自动打开他电脑上的邮件软件，内容已经填好了，
他点发送就行。

**想让留言直接进你的邮箱？** 用 Formspree（免费）：

1. 打开 [formspree.io](https://formspree.io)，注册一个账号
2. 点 **New Form**，创建一个表单，会拿到一个地址，长这样：
   `https://formspree.io/f/abc123xyz`
3. 打开 `js/main.js`，搜索 `mailto:`
4. 找到这段代码：

```js
window.location.href = 'mailto:' + encodeURIComponent(MY_EMAIL) + ...
```

在它**前面加两行**（把 `abc123xyz` 换成你自己的 Form ID）：

```js
fetch('https://formspree.io/f/abc123xyz', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ name: name, email: email, subject: subject, message: message })
});
```

5. 保存，重新部署。之后留言会直接发到你的 Formspree 后台和邮箱。

---

### ❓ 还有什么不懂的

- 代码里所有需要改的地方都标了 **`✏️`**，全局搜它就不会漏
- 每个板块的开头都有中文注释，说明这一块是干什么的
- 实在搞不定，把报错信息复制出来问 AI 或者搜索一下，基本都能解决

---

## 📁 文件结构速查

```
个人网站/
├── index.html          ← 所有内容和结构（7 个板块都在这一个文件里）
├── css/
│   └── style.css       ← 所有样式（改颜色改字体改这里）
├── js/
│   └── main.js         ← 所有交互（改邮箱、改健身数据改这里）
├── images/
│   └── README.md       ← 换照片的详细说明
├── 使用说明.md          ← 你正在看的这份文档
└── overview.md         ← 一页纸交付说明
```

---

祝你玩得开心！有什么想加的功能，随时改 —— 这是你自己的网站 🚀
